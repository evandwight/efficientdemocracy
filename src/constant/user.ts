// key and value must be the same or it wont work
// COLUMNS[value] === value
export enum COLUMNS {
    is_mod = "is_mod",
    wants_mod = "wants_mod",
    send_emails = "send_emails",
    first_run = "first_run",
    is_mini_mod = "is_mini_mod",
}
